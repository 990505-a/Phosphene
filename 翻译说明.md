# Phosphene 中文语言功能 — 使用说明

## 当前状态(2026-08-09)
- ✅ `mlx_ltx_panel.py` 已包含完整中文字典(**528 词**)+ 语言切换功能
- ✅ 设置(Settings)里有 **Language / 语言** 下拉框,选"中文"即时切换
- ✅ 选择保存在浏览器 localStorage,下次打开自动是中文
- ✅ 英文原版完整保留,选 English 随时切回

## 怎么用
1. 打开 Phosphene(http://127.0.0.1:8198)
2. 点 **Settings**(设置)
3. 最顶部 **Language / 语言** → 选 **中文**
4. 界面即时变中文

## Phosphene 更新后怎么恢复中文(重要!)

Phosphene 更新会覆盖 `mlx_ltx_panel.py`,中文功能会丢失。恢复方法:

```bash
cd ~/pinokio/api/phosphene.git
python3 apply_chinese_i18n.py apply
```

然后重启 Phosphene panel:
```bash
# 找到并杀掉旧 panel
kill $(pgrep -f "mlx_ltx_panel")
# 用 LTX 环境的 python 重启
cd ~/pinokio/api/phosphene.git
nohup ltx-2-mlx/env/bin/python3.11 mlx_ltx_panel.py > /tmp/phosphene_panel.log 2>&1 &
```

> ⚠️ 注意:`apply_chinese_i18n.py` 里带的是**精简版字典**(~60 词)。
> 如果要恢复**完整 528 词版**,需要从备份恢复:
> ```bash
> # 完整版备份在这(手动改的)
> cp mlx_ltx_panel.py.bak.i18n.* mlx_ltx_panel.py  # 这是英文原版
> # 然后用完整版替换(见下方"完整版备份"说明)
> ```

## 完整版备份位置
- `mlx_ltx_panel.py` — 当前带 528 词中文的版本(就在用)
- `mlx_ltx_panel.py.bak.i18n.*` — 改之前的英文原版
- `apply_chinese_i18n.py` — 补丁脚本(精简版,用于快速恢复)

## 检查补丁状态
```bash
python3 apply_chinese_i18n.py check
```

## 翻译覆盖范围(528 词)
- 导航栏:当前/队列/历史/日志
- 主按钮:生成/停止/清空/关闭/取消/应用/保存/删除
- 模式:文本/图像/音频/角色/视频
- 参数:画质/引擎/时长/提示词/步数/种子/分辨率
- 画质档:快速/均衡/标准/高
- 训练区:训练类型/数据集/字幕格式/触发词/裁剪策略/语音
- 模型管理:必需/可选/已安装/未安装/就绪
- 状态:加载中/渲染中/空闲/处理中/失败
- 错误提示:上传失败:/下载失败:/网络错误: 等
- 空状态:还没有历史/没有可用的风格模型 等
